<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="test">

  <resultMap id="result-contact" class="java.util.HashMap">
    <result property="id" column="id"/>
    <result property="lastName" column="lastName"/>
    <result property="firstName" column="firstName"/>
    <result property="email" column="email"/>
    <result property="rating" column="rating"/>
  </resultMap>

  <select id="contact-get-feed" resultMap="result-contact">
    select * from "Contact"
  </select>

  <select id="contact-get-entry" resultMap="result-contact">
    select * from "Contact" where "id" = #value#
  </select>

  <delete id="contact-delete-entry" >
    delete from "Contact" where "id" = #value#
  </delete>
  
  <insert id="contact-insert-entry" parameterClass="map">
    insert into "Contact" ("lastName", "firstName", "email", "rating") values
        (#lastName#, #firstName#, #email#, #rating:NUMERIC#)
    <selectKey keyProperty="id" resultClass="long">
      SELECT currval('"Contact_id_seq"')
    </selectKey>
  </insert>

  <update id="contact-update-entry" parameterClass="map">
    update "Contact" set "lastName" = #lastName#, "firstName" = #firstName#,
        "email" = #email#, "rating" = #rating:NUMERIC# where "id" = #id#
  </update>

</sqlMap>
